<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HealthEase - Login</title>
    <style>
        body{
            height: 100vh;
            width: 100vw;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            background-color: #283618;
            color: #fefae0;
            font-family: monospace;
            overflow: hidden;
        }


        .authCard{
            height: 60vh;
            width: 28vw;
            border-radius: 2vw;
            background-color: #606c38;
            display: flex;
            justify-content: space-evenly;
            align-items: center;
            flex-direction: column;
        }
        .authCard h1{
            font-size: 2.5vw;
            margin: 0;
        }
        .authCard hr{
            border: 1px solid #fefae0;
            width: 24vw;

        }


        .authCard form{
            display: flex;
            /* align-items: center; */
            justify-content: space-between;
            flex-direction: column;
            font-size: 1.2vw;

        }
        .authCard form label br{
            margin: 0 1vh;
        }
        .authCard form input{
            margin: 1vh 0;
            width: 24vw;
            border-width: 0 0 2px 0;
            border-color: black;
            font-size: 1.2vw;
            color: black;
            background-color: #606c38;
            outline: none;
            color: #fefae0;

        }

        .auth-btns{
            width: 24vw;
            height: 3.5vw;
            background-color: #283618;
            font-size: 2vw;
            border: none;
            cursor: pointer;
            &:hover{
                background-color: #333c16;
            }
            color: #fefae0;
            border-radius: 2vw;

        }
    </style>
</head>
<body>
    <div class="authCard">
        <h1>Login</h1>
        <hr>

        <% if(locals.error) { %>
            <p><%= locals.error %> </p>
        <% } %>

        <form action="/login" method="post">

            <label for="email">Enter Your Email :<br> <input type="text" name="email" id="email"></label>
            <br>
            <label for="password">Enter Your Password :<br> <input type="password" name="password" id="password"></label>
            <br>

            <button type="submit" class="auth-btns">Login</button>
        </form>
    </div>
</body>
</html>