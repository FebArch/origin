<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= data.username %> - Profile Set Up
    </title>
    <style>
        body {
            height: 100vh;
            width: 100vw;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #f4f4f4; /* Light background */
            color: #333; /* Dark text */
            font-family: 'Arial', sans-serif;
            margin: 0;
            overflow: hidden;
        }

        .profileCard {
            background-color: #ffffff; /* White background for the card */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            text-align: center;
            animation: fadeIn 0.5s ease;
            width: 400px; /* Fixed width for the card */
        }

        h1 {
            color: #4CAF50; /* Green */
            margin-bottom: 20px;
            font-size: 24px;
            transition: color 0.3s ease;
        }

        label {
            display: flex;
            align-items: center; /* Center items vertically */
            margin: 10px 0;
            font-size: 18px;
        }

        .formGroup {
            display: flex;
            justify-content: space-between;
            margin: 10px 0;
        }

        input[type="text"], input[type="number"], textarea {
            padding: 10px;
            margin-top: 5px;
            border: 2px solid #ccc;
            border-radius: 5px;
            width: 48%; /* Make inputs share the row equally */
            transition: border-color 0.3s ease;
        }

        input[type="text"]:focus, input[type="number"]:focus, textarea:focus {
            border-color: #4CAF50; /* Green border on focus */
            outline: none;
        }

        input[type="radio"] {
            margin-right: 10px; /* Space between radio button and label */
        }

        button {
            background-color: #4CAF50; /* Green */
            color: #ffffff; /* White text */
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 18px;
            transition: background-color 0.3s ease, transform 0.2s ease;
            margin-top: 20px; /* Space above the button */
        }

        button:hover {
            background-color: #45a049; /* Darker green on hover */
            transform: scale(1.05);
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Media Query for responsiveness */
        @media (max-width: 600px) {
            .profileCard {
                width: 90%;
            }

            h1 {
                font-size: 20px;
            }

            input[type="text"], input[type="number"], textarea {
                width: 100%; /* Full width on smaller screens */
            }

            .formGroup {
                flex-direction: column; /* Stack fields vertically */
            }
        }
    </style>
</head>

<body>
    <div class="profileCard">
        <h1>
            <%= data.username %> - Profile
        </h1>
        <hr>
        <form action="" method="post">
            <p class="genderCard">
                <label for="male">
                    <input type="radio" name="gender" id="male" value="male"> Male
                </label>
                <label for="female">
                    <input type="radio" name="gender" id="female" value="female"> Female
                </label>
                <label for="others">
                    <input type="radio" name="gender" id="others" value="others"> Others
                </label>
            </p>

            <div class="formGroup">
                <label for="height">Height (cm):</label>
                <input type="number" id="height" name="height" required>
            </div>
            <div class="formGroup">
                <label for="weight">Weight (kg):</label>
                <input type="number" id="weight" name="weight" required>
            </div>

            <div class="formGroup">
                <label for="age">Age:</label>
                <input type="number" name="age" id="age" required>
            </div>

            <p class="medicalHistoryCard">
                <label for="medicalHistory">Medical History:</label>
                <textarea id="medicalHistory" name="medicalHistory" rows="3" placeholder="e.g. diabetes, asthma..."></textarea>
            </p>

            <p class="allergiesCard">
                <label for="allergies">Allergies:</label>
                <textarea id="allergies" name="allergies" rows="2" placeholder="e.g. pollen, nuts..."></textarea>
            </p>

            <div class="formGroup">
                <label for="contact">Emergency Contact Number:</label>
                <input type="text" id="contact" name="contact" required>
            </div>

            <button type="submit">Submit</button>
        </form>
    </div>
</body>

</html>
